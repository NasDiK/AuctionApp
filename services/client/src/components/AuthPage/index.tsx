import {Button} from '@sharedComponents';
import {loadingStatus} from '@local/enums/shared';

const AuthPage = () => {
  return <div>
    <span>{'Авторизация'}</span>
    <Button>{'hueta'}</Button>
  </div>
};

export default AuthPage;